    <h3>Narrow By </h3> <br />
 
 {%if 'selected_facets' not in request.build_absolute_uri %}
 {% if facets.fields.category %}
                
                <h4>Department</h4>
               {% for category in facets.fields.category %}
                    <a href="{{ request.get_full_path }}&amp;selected_facets=category_exact:{{ category.0|urlencode }}">{{ category.0 }}</a> ({{ category.1 }})
               {% endfor %}
                <br />
 {% endif %}
 
 {%else%}
  {% if facets.fields.subcategory %}
               <h4>Sub Category</h4>
               {% for subcategory in facets.fields.subcategory %}
                    <a href="{{ request.get_full_path }}&amp;selected_facets=subcategory_exact:{{ subcategory.0|urlencode }}">{{ subcategory.0 }}</a> ({{ subcategory.1 }}) <br />
               {% endfor %}
               
 {% endif %}
 
  {% endif %}
 

